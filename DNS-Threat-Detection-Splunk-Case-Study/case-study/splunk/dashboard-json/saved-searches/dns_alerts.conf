[ALERT - Large DNS Query Length]
search = index=dns sourcetype=dns:log | eval domain_length=len(domain) | where domain_length>50 | stats count by src_ip domain
cron_schedule = */5 * * * *
alert_type = always
actions = email, webhook
